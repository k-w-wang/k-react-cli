import * as React from "react";

import Error from "../components/Error";

/**
 * 路由 - 表格页
 * @constructor
 */
const ErrorRoute: React.FC = (props: any) => {
  return <Error {...props} />;
};

export default ErrorRoute;